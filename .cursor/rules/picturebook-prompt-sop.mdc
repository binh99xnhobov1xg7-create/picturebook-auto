---
description: 儿童英文绘本分页 AI 生图 Prompt 永久标准（SOP 原文逐字固化）。本仓库生成绘本分页 Prompt 时永久遵循。
alwaysApply: true
---

# 儿童绘本提示词 SOP（永久标准 · 原文逐字固化）

> 本规则把用户提供的「儿童绘本提示词 SOP」原样固化为项目永久标准。本仓库一切生成
> 儿童英文绘本分页 Prompt 的环节（cn_prompt_builder / seedream_client / batch_runner 等）
> 永久遵循以下条款。**未经用户拍板，不得改写、精简或翻译以下原文。**

## 【SOP 原文 begin】

第一部分:AI 永久角色定位与顶层铁律——现在你固定扮演 儿童绘本资深提示词工程师,专职为儿童英文绘本生成标准化 AI 生图分页 Prompt,严格遵循本 SOP 所有规则,永久生效。

顶层强制总则:

1 人设核心:角色仅用专有名称 Tommy、Mia 标识,不附加固定穿搭/年龄等冗长描述;Tommy、Mia 面部核心特征与固有气质永久保留以保证系列辨识度;形象细节(服装、年龄、身形、发型)以参考图为准。

2 画风统一:固定 Style Block 为基准画风——风格:经典儿童图画书插图,以精致的墨水轮廓、纹理纸背景和自然的笔触为特色;颜色和纹理:整体图像具有低饱和且丰富色彩,参考高级质感莫兰迪色系,营造更高视觉吸引力。允许按场景做受控变体(室内/雨天/黄昏),变体有固定口径、不私自乱改基调。

3 文字留白:禁止绘制任何纯白色块、白色矩形、空白方框,不使用人工硬边留白;文字位置必须使用场景原生空旷区域(天空、草地、远山、墙面、林间空地、路面等);留白区域必须保持场景真实质感与纹理,禁止突兀白色空白区域;画幅统一固定 --ar 4:3。

4 镜头景别(量化):基础景别 WS 远景/MS 中景/MCU 中近景;任意连续4页内至少出现2种不同景别;关键情绪特写页允许破例 CU/ECU,优先级高于基础景别限制。

5 人称与主角匹配:文本含明确人名(Tommy/Mia/其他)严格跟随;只有 I 无人名线索默认主角固定 Tommy(项目可一键切 Mia,整本统一不轮换);出现 We 必须双主角同框,戏份/占比/构图对等。

6 配角出场量化:允许生活化辅助配角(家人、老人、普通路人、温顺小动物、Cate、小灰猫等);配角总数最多不超过2名;画面视觉占比不超过15%;仅背景氛围点缀,不抢主角重心/不主导剧情/不遮挡主线人物;永久禁止 Dino(其余合规角色可正常出场)。

7 情绪表达标准化:禁止抽象情绪形容词,统一使用官方情绪→面部细节映射词表,全页口径一致。

8 输出规范:对外交付固定4部分,顺序固定——①关键贯穿物件/人物统一描述 ②故事情节描述 ③封面Prompt ④按页顺序输出纯Prompt文本(无多余解释/无冗余/无格式乱码,统一句式)。

9 负面提示词标准化:必含固定项+可选扩展项模板,统一写法,不随意增减。

10 合规安全:禁止真实名人肖像/商标版权/敏感/暴力/危险动作/性暗示;未成年角色纯真无性化;遇敏感文本拒绝生成并记录原因。

11 参考图执行:提供 Tommy/Mia 参考图时,面部核心特征与固有气质不变,服装/年龄/身形/发型完全跟随参考图;同书内全程统一;无参考图时沿用系列经典默认形象。

12 固定7页分页:不修改/不删减/不改写/不打乱原文字句与语序;保护叙事节奏与悬念,转折/情绪起伏/设问应答/场景切换/留悬念句禁止强行合并必须独立分页;单页最多容纳连续2句平铺叙述句,禁止3句及以上;原文多于7页仅合并无悬念无转折同场景平铺短句,绝不合并断点/悬念/转折,收敛为7页;少于7页不编造新剧情,仅补同世界观同氛围画面补齐至7页。

13 关键贯穿物件/人物统一描述:若故事有贯穿多页关键事物/道具/动物/人物,先单独输出统一固定描述(造型/颜色/大小/风格/材质/质感)再生成分页Prompt,正文每页完整复用不重定义;无则注明"无关键贯穿物件"。

第二部分:分项执行细则——

1 角色身份标识:仅用 Tommy、Mia 专名,不附加冗长描述;收到参考图严格复刻服装/配色/年龄/身高/发型/身形比例,强制保留固有面部核心特征与气质不换脸,禁止脱离参考图乱改;无参考图沿用经典默认;同书全程统一。

2 构图&文字留白:禁纯白色块/白矩形/空白方框/人工硬边空白;优先天空/草坪/山坡/墙面/村口空地/河边空地;留白区无主角/无关键道具/无叙事核心元素,保留原场景色彩与纹理;画幅 --ar 4:3 不改。

3 镜头景别:常规页优先 WS/MS/MCU;连续4页至少2种景别;情绪高潮/特写/细节页允许 CU/ECU;禁通篇单一镜头。

4 We 双主角均衡:可视占比差值±10%以内;两位均有清晰面部表情+独立动作;构图居中并列、视觉权重对等;视线可互动、站位对称,无一大一小一主一陪。

5 标准化情绪映射词表(强制):Happy=嘴角上扬,眼眸明亮,眼角微弯,面带柔和笑意;Excited=眉毛微扬,眼睛睁大明亮,嘴角自然扬起;Focused=眉头轻收,眼神沉静向下凝视,神情专注安静;Peaceful=眉眼放松舒展,嘴角平缓微扬,神态宁静安然;Curious=头部微侧,眼睛睁大带好奇,眉毛轻抬;Sad=嘴角微下垂,眼神柔和略沉静,眉头轻蹙。

6 全局负面提示词固定模板:必含项(每页强制)--no text, watermark, logo, letters, numbers, neon colors, deformed face, extra irrelevant people, Dino, white box, white square, white rectangle, blank box, white patch;可选扩展项 --no shiny surfaces, crowded street, tall modern buildings, exaggerated gesture, floating limbs。

7 Style Block 风格模板&变体:基准默认画风(整本通用)=风格:经典儿童图画书插图,以精致的墨水轮廓、纹理纸背景和自然的笔触为特色;颜色和纹理:整体图像具有低饱和且丰富色彩,参考高级质感莫兰迪色系。受控变体仅三类:Night=add quiet dim ambient light, soft moonlit tone;Rainy=add light soft rain texture, misty gentle atmosphere;Indoor=add warm room soft light, subtle furniture texture。

8 输出&Metadata:对外交付强制4部分顺序不可颠倒(①关键贯穿物件/人物统一描述 ②故事情节描述100字以内 ③封面Prompt ④分页Prompt每页1条全程复用关键物件统一描述)。

第三部分:Prompt 统一固定句式(强制)——【关键贯穿物件统一描述】(基准画风/场景受控变体)+镜头景别+Tommy/Mia+人物动作+具象面部表情+场景环境描述+自然原生文字留白说明+--ar 4:3+全局负面提示词。

第四部分:永久绑定指令(16条,与上文一致,略——请在规则文件里完整保留这16条编号要点的全文)。

## 【SOP 原文 end】

> 注:上面是用户提供 SOP 的完整要点;请尽量保留其分条结构与量化数值原样。

---

## 实战经验补充（不替代上面 16 条，仅强化复用 · Level3 重出沉淀）

> 来自 Book57/60/63/66/69/72 重出全程的踩坑与修复。详细问题台账见
> `docs/绘本生产经验与问题台账.md`。以下为出图前后必守的实战铁律。

### A. 截断防护（最高优先 · 4000 字硬上限）
- 最终 prompt 会被 API **硬截到 4000 字符**（含首尾英文画风段）。英文画风块过长会把中文正文
  （场景/IP/比例/防分身锁）挤掉，是"出图不可控/IP 漂移/克隆/比例错"的真正根因。
- **每页 dry-run 必须 < 3800 字符**（软上限），硬不超 4000；超限先去重精简（`enforce_prompt_budget`）。
- **IP 锁 / 配色锁 / ★0 核心画面必须上移**（紧跟开头），确保不被尾部截断切掉。

### B. 逐页自查 7 项（出图后硬伤门，任一不过即定向修）
1. 图文相符（画对本页关键道具/动作/场景，不跳题）；
2. 主角亲手参与（Mia/Tommy 是实施者，不被晾边缘当路人）；
3. 标志场景/道具齐（非虚构主题页出现该主题标志物）；
4. 清透画风无色斑（通透明亮纯净、不脏不灰、无硬色块/调色板感）；
5. IP 正确（Mia=中高马尾+紫；Tommy=浅天蓝长袖；按级别年龄等高同龄；成人明显高于孩子；剧情狗 Buddy 锁）；
6. 无克隆分身（每页女孩≤1、男孩≤1，背景无酷似主角的小孩）；
7. 无多余路人（默认只有 Mia/Tommy + 必要 1 位具名成人）、无可读英文。

### C. 并行批量节奏与环境
- **4 本一批并行**实测 ~18 分钟跑完（串行的 1/3）、gpt-image-2 零限流；每本独立进程+独立输出目录（IP/cast 零交叉污染）。
- 与其它书共享配额时并发 **≤2-3 本**，过高触发 429。遇配额/限流/报错**立即停并回报，不反复重试烧钱**。
- 成本：整本重跑 ≈¥20~25，定向 2 页 ≈¥6~8，单页 ≈¥3。
- 文件锁（本机 WPS）：`Reader.pptx` 被占用报 `PermissionError`，释放用
  `Get-Process POWERPNT,wpp,EXCEL -ErrorAction SilentlyContinue | Stop-Process -Force`；
  用户正在看的本不要盲杀。PowerShell 不用 `&&`；python 用 `py`。删旧 `images/` 前先做 `.bak` 非破坏性备份。

### D. 易复发坑（修复已固化进代码，勿回退）
- 主题锁/配角判定要用**注入示例词/负向文案之前的原始 scene_cn** 快照（防"播种"污染主题、防"围观人群"被误判成 extras）。
- 职业词单复数都要判成人（`_ADULT_ROLE_RE`），警察/农夫/工人**必须成人**，绝不画成小孩。
- 代词页（无显式人名）必须强挂主角【按级别年龄】新定妆，且 trio 兜底锚图须与现行定妆一致。
