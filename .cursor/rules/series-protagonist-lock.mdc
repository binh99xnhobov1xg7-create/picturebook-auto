---
description: Dino Online Club 系列主角铁律（Mia & Tommy 锁定）——任何绘本/出图/教辅生成都必须遵守
alwaysApply: true
---

# Dino Online Club · 主角铁律

完整设定见 `references/series_bible.md`（SSOT）。运行时精确值见 `scripts/character_registry.py`
与 `assets/ip_library/_manifest.json`。下面是绝不可违背的核心：

## 1. 双主角
- **Dino Online Club（0-6 级）是以兄妹 Mia（妹妹）+ Tommy（哥哥）为核心的连续系列。每一本书都必须出现他俩，一切围绕他俩。**
- 出现别的角色（Anna / Grandma / 妈妈爸爸…）时，**他们是配角；Mia 和 Tommy 仍是主角且必须在场**。
- 故事出现"家人/妈妈/爸爸/爷爷/奶奶" → 配套的孩子必然是 Mia & Tommy。

## 2. 年龄锁（含身高）
| 级别 | 主角年龄 | 定妆 key |
|---|---|---|
| L0-2 | **8 岁** | `mia_8` / `tommy_8` |
| L3-4 | **10 岁** | `mia_10` / `tommy_10` |
| L5-6 | **12 岁** | `mia_12` / `tommy_12` |

"形象" = 脸 + 五官 + 发型 + 服装与配色 + **身高/身材比例**，全部按该年龄锁死，禁止跨档错位
（如把 L3/L4 画成 12 岁）。Mia=单束高马尾+紫色；Tommy=蓬松短发+蓝色；均不戴眼镜/饰品。

## 3. 反错位三条
- **不分身**：同一角色一张图只出现一次（禁双 Mia / 双 Tommy / 分身 / 镜像）。
- **不乱入**：不许凭空新增陌生小孩；未点名的已登记角色（如 Anna）不得出现。
- **Dino 不进画面**：所有级别 Dino 只作品牌 logo，绝不当故事角色。

## 4. 角色身份四层判定（所有书通用）
①系列 IP 定妆锁(按级别年龄) → ②官方逐页"在场名单"为权威(剔否定句、只认专名 Ms./Teacher Kim) →
③一次性/非 IP 角色=永远是自己(护士/Goldilocks/狐狸/Ravi…)，**绝不串成 Mia/Tommy**；反复出场→书内角色册锁(统一描述+书内定妆锚图) →
④无名泛指小孩 girl→Mia / boy→Tommy → ⑤动物/反派→儿童向柔化(friendly 可爱，去獠牙凶相)。

## 5. 框架式寓言（读者≠故事角色）—— 默认方案 B
寓言里 Mia/Tommy 是**读者**，**绝不进入故事场景**。默认 **方案 B**：每页"读者缩在左下角看书(约20-25%) + 故事发光幻象铺满画面(主体约75%)"，
幻象与读书区用细光晕过渡、除读者一角外不留空白。新书纯动物寓言(无人类主角/家人)自动按框架处理。

## 6. 故事连续性
画面内容以**官方逐页场景**为权威源；自动把同一场天气(雨/雪/夜)贯穿到区间内所有户外页，**绝不中间页突然放晴**。

## 7. 新书两种输入都要自动适配
- 带官方逐页 Prompt → 以官方在场名单/场景为权威。
- 只有故事正文 → AI 解析逐页场景 + 上述全部铁律(book_cast/框架/连续性均有纯文本回退)。

## 8. 取舍顺序（防幻觉）＋ 自检门
官方图像参考 > 本铁律 / series_bible > 官方文本(S&S/每课Prompt/世界观) > 代码 SSOT > 模型补全(需标注)。
**先取证再生成**；出图前过自检门(防分身/Dino/缺主角/错龄/超员)，违规标"人工抽查"。
改主角/年龄规则时先改代码 SSOT 再同步 `series_bible.md`。
